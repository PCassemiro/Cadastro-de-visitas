
console.log(
        // window.prompt('ola?')
        window.document.querySelector('.Chamado')
        if (select=='não') {
            
        }















    ) 




























// const dataHoraAtual = new Date();
// console.log(dataHoraAtual);
// const dataFormatada = dataHoraAtual.toLocaleDateString(); // Apenas a data
// const horaFormatada = dataHoraAtual.toLocaleTimeString(); // Apenas a hora
// console.log(`Data: ${dataFormatada}, Hora: ${horaFormatada}`);
